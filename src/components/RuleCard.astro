---
interface Props {
  rule: string;
  description: string;
  process: string;
  images?: string[];
  href?: string;
  transitionName?: string;
}

const { rule, description, process, images = [], href, transitionName } = Astro.props;

const ruleUrl = href || '#';
---

<a href={ruleUrl} class="rule-card-link">
  <article class="rule-card" transition:name={transitionName}>
    <header class="rule-card__header">
      <h2 class="rule-card__title">"{rule}"</h2>
    </header>
  </article>
</a>

<style>
  .rule-card-link {
    text-decoration: none;
    color: inherit;
    display: block;
    transition: opacity 0.3s ease;
  }

  .rule-card {
    background-color: var(--color-background);
    padding-block: 1rem;
    border: 1px solid var(--color-accent);
    border-radius: 5px;
    cursor: pointer;
    margin-bottom: var(--margin);
    margin-left: var(--margin);
    font-variation-settings: "wght" 200, "wdth" 110, "slnt" 0;

  }
  .rule-card:hover {
    transition: all 0.3s ease-in-out;
    scale: 1.01;
    box-shadow:  0px 14px 84px 0px rgba(0, 0, 255, 1);
    font-variation-settings: "wght" 700, "wdth" 110, "slnt" 0;


  }

  .rule-card__header {
    margin: var(--margin)
    }

  .rule-card__title {
    font-family: var(--font-headings);
    font-size: clamp(2rem, 6vw, 5rem);
    line-height: 1.05;
    text-transform: uppercase;
    letter-spacing: -0.02em;
  }
</style>
